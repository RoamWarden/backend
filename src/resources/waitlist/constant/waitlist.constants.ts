/** Default page number for the admin waitlist listing. */
export const WAITLIST_DEFAULT_PAGE = 1;

/** Default page size for the admin waitlist listing. */
export const WAITLIST_DEFAULT_LIMIT = 50;

/** Hard cap on page size for the admin waitlist listing. */
export const WAITLIST_MAX_LIMIT = 200;
